# Beispiel Umgebungsvariablen — kopiere in eine sichere .env (nicht ins Repo ohne Schutz)
# OpenRouter
OPENROUTER_API_KEY=
# OpenAI
OPENAI_API_KEY=
# Lokales Modell (z. B. Ollama) - wenn verwendet
CLINE_LOCAL_URL=http://localhost:11434
# Optional: Name der zu verwendenden Model-Engine (Provider-spezifisch)
CLINE_MODEL=gpt-4o-mini
